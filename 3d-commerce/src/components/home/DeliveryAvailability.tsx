"use client";
import { useState } from "react";
import { IconCheck, IconMapPin, IconX } from "@tabler/icons-react";
export function DeliveryAvailability(){
const [postalCode,setPostalCode]=useState("");const [loading,setLoading]=useState(false);const [result,setResult]=useState<{deliverable:boolean;message:string}|null>(null);
async function check(e:React.FormEvent){e.preventDefault();if(!postalCode.trim())return;setLoading(true);setResult(null);try{const r=await fetch("/api/delivery/check/"+encodeURIComponent(postalCode.trim()),{cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d?.message||"Unable to check delivery.");setResult(d)}catch{setResult({deliverable:false,message:"Delivery availability could not be checked right now."})}finally{setLoading(false)}}
return <section className="border-y border-border bg-surface/40"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-4 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8"><div className="flex items-start gap-3"><span className="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-primary/10 text-primary"><IconMapPin size={15}/></span><div><p className="text-[9px] uppercase tracking-[0.18em] text-primary">Delivery availability</p><p className="mt-1 text-xs text-muted">Check whether we deliver to your postal / PIN code before ordering.</p></div></div><form onSubmit={check} className="flex w-full max-w-md gap-2 sm:w-auto"><input value={postalCode} onChange={e=>setPostalCode(e.target.value)} inputMode="numeric" placeholder="Enter PIN code" className="h-10 min-w-0 flex-1 rounded-xl border border-border bg-background px-3 text-xs outline-none focus:border-primary/50 sm:w-44"/><button disabled={loading} className="h-10 rounded-xl bg-foreground px-4 text-xs font-semibold text-background disabled:opacity-50">{loading?"Checking…":"Check"}</button></form>{result&&<div className={result.deliverable?"flex items-center gap-2 text-xs text-foreground":"flex items-center gap-2 text-xs text-red-300"}>{result.deliverable?<IconCheck size={15}/>:<IconX size={15}/>}<span>{result.message}</span></div>}</div></section>
}